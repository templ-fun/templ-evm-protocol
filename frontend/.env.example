# Frontend environment overrides for templ
# Copy to .env.local and adjust as needed.

# Base URL for API requests (defaults to http://localhost:3001 when unset)
VITE_BACKEND_URL=http://localhost:3001

# Must match backend BACKEND_SERVER_ID to validate typed data
VITE_BACKEND_SERVER_ID=templ-dev

# Optional: point at a specific factory when deploying via the UI
# VITE_TEMPL_FACTORY_ADDRESS=0x0000000000000000000000000000000000000000
# VITE_TEMPL_FACTORY_DEPLOYMENT_BLOCK=0
# VITE_TEMPL_FACTORY_PROTOCOL_RECIPIENT=0x0000000000000000000000000000000000000000
# VITE_TEMPL_FACTORY_PROTOCOL_PERCENT=10

# Optional: override read provider
# VITE_RPC_URL=http://127.0.0.1:8545

# Enable extra UI helpers during e2e runs
VITE_E2E_DEBUG=0
