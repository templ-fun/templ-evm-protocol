testMode: property
testLimit: 1000
seqLen: 500
shrinkLimit: 100
workers: 1

cryticArgs:
  # remapping for OpenZeppelin imports used in contracts
  - --solc-remaps
  - '@openzeppelin/=node_modules/@openzeppelin/'
  # enable via-IR and optimizer to avoid stack-too-deep on complex constructors
  - --solc-args
  - '--via-ir --optimize --optimize-runs 200'
