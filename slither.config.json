{
  "fail_on": "medium",
  "solc_remaps": [
    "@openzeppelin=node_modules/@openzeppelin"
  ],
  "detectors_to_exclude": [
    "arbitrary-send-eth"
  ],
  "_note": "Arbitrary external execution has been removed; test helpers are eliminated and analysis targets contracts/TEMPL.sol only.",
  "_analysis": "npm run slither analyzes only contracts/TEMPL.sol; test helpers are excluded"
}
