RPC_URL=http://localhost:8545
PORT=3001
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173
BACKEND_SERVER_ID=templ-dev
APP_BASE_URL=http://localhost:5173

# XMTP Configuration
XMTP_ENABLED=0
XMTP_ENV=dev
# BACKEND_DB_ENC_KEY=0x... # 32-byte hex key for encrypting XMTP database (generate in production)
# BOT_PRIVATE_KEY=0x... # Optional: provide your own bot private key
XMTP_MAX_ATTEMPTS=20
XMTP_BOOT_MAX_TRIES=30
FAST_XMTP=1 # Set to 1 for faster testing
ALLOW_DETERMINISTIC_INBOX=1
VERIFY_CONTRACTS=0
DISABLE_XMTP_WAIT=1 # Disable XMTP wait for tests
