## `JoinCaller`

Helper contract that attempts to call TEMPL's join entry point.




### `constructor(address _templ)` (public)



Store TEMPL address at construction

### `callJoin()` (external)

Call TEMPL.join() from this helper.






