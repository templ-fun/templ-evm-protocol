## `TestToken`



Simple ERC20 token with mint and burn capabilities for testing


### `constructor(string name_, string symbol_, uint8 decimals_)` (public)



Construct a test ERC20 with custom decimals

### `mint(address to, uint256 amount)` (external)

Mint tokens to an address (testing only)






