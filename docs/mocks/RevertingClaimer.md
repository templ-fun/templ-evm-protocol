## `RevertingClaimer`



Helper member that reverts on ETH reception to exercise failing claim paths.


### `joinTempl(address templ, address token, uint256 amount)` (external)

Join the provided TEMPL instance by approving and calling join.



### `claimExternal(address templ, address token)` (external)

Claim an external reward token from the templ.



### `receive()` (external)








